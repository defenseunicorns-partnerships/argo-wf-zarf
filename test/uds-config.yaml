variables:
  uds-k3d-dev:
    minio:
      set:
        buckets:
          - name: "argo"
            policy: "public"
            purge: false
        users:
          - accessKey: test-user
            secretKey: "test-secret"
            policy: consoleAdmin
