variables:
  uds-k3d-dev:
    set:
      persistence:
        size: 5Gi
      buckets:
        - name: "argo"
          policy: "public"
          purge: true
      users:
        - accessKey: test-user
          secretKey: "test-secret"
          policy: consoleAdmin
