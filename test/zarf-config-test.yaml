package:
  deploy:
    set:
      NAMESPACE: argo
      DEPLOY_POSTGRESQL: true
      PG_USER: argo
      PG_USER_PASSWORD: argo
      PG_DB: argo-workflows
      PG_HOST: argo.postgresql-0.argo.svc.cluster.local
      PG_PORT: 5432
      PG_PASSWORD: pgadmin
      S3_ENDPOINT: minio.uds-dev-stack.svc.cluster.local
      S3_ACCESS_KEY: test-user
      S3_SECRET_KEY: test-secret
