kind: ZarfPackageConfig
metadata:
  name: argo-workflows-testing
  description: Zarf package for testing argo workflows
components:
  - name: argo-workflows-testing
    required: true
    description: WorkflowTemplate for testing argo workflows
    manifests:
      - name: argo-workflows-testing
        files:
          - hello-world-template.yaml
          - test.yaml
    images:
      - bitnami/postgresql:15.6.0
      - localhost:45000/legit-workflow:latest
